{
  "scripts": {
    "start": "parcel ./src/index.html -p 1234",
    "watch": "bsb -clean-world -make-world -w",
    "build": "parcel build --public-url / ./src/index.html"
  },
  "dependencies": {
    "@ahrefs/bs-recharts": "^0.2.4",
    "reason-react": "^0.5.3"
  },
  "devDependencies": {
    "bs-platform": "^4.0.5",
    "parcel-bundler": "^1.10.1"
  }
}
